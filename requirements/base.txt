fastapi>=0.115.0
fastapi-cache2>=0.2.1
Pillow>=10.0.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.19.0
alembic>=1.13.0
ultralytics>=8.0.20
opencv-python-headless>=4.11.0
# CPU inference runtime for the optimized data/models/ppe.onnx export — see
# backend/detection/detector.py _resolve_model_backend(). Falls back to the
# .pt weights at startup if this isn't installed.
onnxruntime>=1.18.0
# torch and torchvision are NOT listed here — they must be installed first from
# the CPU-only wheel index (https://download.pytorch.org/whl/cpu) before this
# file is processed.  Listing them here would let pip re-resolve them against
# default PyPI and pull CUDA wheels.  See docker/Dockerfile.backend.
numpy>=1.26.0
aiosmtplib>=3.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
paho-mqtt>=1.6.1
deepface>=0.0.93
tensorflow==2.21.0
tf-keras==2.21.0
scipy>=1.11.0
pandas>=2.0.0
redis>=5.0.0
lap>=0.5.12
reportlab>=4.0.0
qrcode[pil]>=7.4.2
aiortc>=1.9.0
av>=12.0.0
