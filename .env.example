# SafeSite AI — environment variable template (placeholders only, never real secrets).
# Copy the values you need into your local .env (which is git-ignored).

# ── n8n Payroll Risk Analysis Agent ───────────────────────────────────────────
# Shared secret sent by the n8n workflow in the X-N8N-API-KEY header on the agent
# execution endpoints (/api/v1/admin/payroll/agent/risk-analysis and /risk-analysis-log).
# Generate a long random string. The React frontend never uses this value.
N8N_PAYROLL_AGENT_API_KEY=_HfICEmSNroJ9thzyiHLbr-JBPPfyfmlynonL6RXNZ-hkORO4nEFG1EAK23gAkjY
